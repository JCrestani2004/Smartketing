---
const { href, ...props } = Astro.props;
const base = import.meta.env.BASE_URL;
// Esto limpia las barras dobles por si acaso
const cleanHref = `${base}/${href}`.replace(/\/+/g, '/');
---
<a href={cleanHref} {...props}>
  <slot />
</a>